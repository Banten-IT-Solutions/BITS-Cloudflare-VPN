interface:
  display_name: "BITS Cloudflare VPN"
  short_description: "Repo-specific patterns and workflows for BITS-Cloudflare-VPN"
  default_prompt: "Use the BITS-Cloudflare-VPN repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true